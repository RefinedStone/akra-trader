// @ts-nocheck
export function RuntimeProviderProvenanceSchedulerStitchedReportApprovalQueuePreviewHeadlineSummarySection({
  model,
  plan,
}: {
  model: any;
  plan: any;
}) {
  return (
    <strong>{formatProviderProvenanceSchedulerNarrativeGovernancePlanSummary(plan)}</strong>
  );
}
