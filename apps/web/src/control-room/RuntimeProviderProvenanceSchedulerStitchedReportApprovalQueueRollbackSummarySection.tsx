// @ts-nocheck
export function RuntimeProviderProvenanceSchedulerStitchedReportApprovalQueueRollbackSummarySection({
  model,
  plan,
}: {
  model: any;
  plan: any;
}) {
  return <p className="run-lineage-symbol-copy">{plan.rollback_summary}</p>;
}
