// @ts-nocheck
export function RuntimeProviderProvenanceSchedulerStitchedReportPolicyCatalogActionCellSection({
  model,
  children,
}: {
  model: any;
  children: any;
}) {
  const {} = model;

  return (
    <td>
      <div className="market-data-provenance-history-actions">{children}</div>
    </td>
  );
}
