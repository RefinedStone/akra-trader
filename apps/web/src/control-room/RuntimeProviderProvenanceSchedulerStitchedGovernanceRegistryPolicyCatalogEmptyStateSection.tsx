// @ts-nocheck
export function RuntimeProviderProvenanceSchedulerStitchedGovernanceRegistryPolicyCatalogEmptyStateSection({
  model,
}: {
  model: any;
}) {
  const {} = model;

  return (
    <p className="empty-state">No stitched governance registry policy catalogs match the current search.</p>
  );
}
